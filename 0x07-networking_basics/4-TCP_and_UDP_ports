#!/usr/bin/env bash
# This script displays the listening ports on the system.
# It also shows the Process ID (PID) and the name of the program associated with each socket.

# Explanation of functionality:
# - The script identifies and lists the ports that are in a listening state.
# - It also provides information about the associated process using the PID and program name.

netstat -l --program